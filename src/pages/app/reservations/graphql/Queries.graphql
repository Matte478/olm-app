query Devices {
  devices {
    ...DeviceWithReservations
  }
}

query ServersWithDevices($production: Boolean) {
  servers(production: $production) {
    production
    devices {
      ...DeviceWithReservations
    }
  }
}
