query Devices($reservationStart: DateTimeRange) {
  devices {
    ...DeviceWithReservations
  }
}

query ServersWithDevices($production: Boolean, $reservationStart: DateTimeRange) {
  servers(production: $production) {
    production
    devices {
      ...DeviceWithReservations
    }
  }
}
